# =============================================================================
# AI-Swarm Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# See docs/BOT_ACCOUNT_SETUP.md for detailed setup instructions.

# -----------------------------------------------------------------------------
# Atlassian (Required) — shared for Jira and Confluence
# -----------------------------------------------------------------------------
ATLASSIAN_URL=https://your-domain.atlassian.net
CONFLUENCE_URL=https://your-domain.atlassian.net/wiki  # Must include /wiki suffix

# Bot account (recommended for audit trail)
ATLASSIAN_BOT_EMAIL=ai-executor-bot@yourcompany.com
ATLASSIAN_BOT_API_TOKEN=your-bot-api-token

# Admin account (fallback — not recommended for production)
# ATLASSIAN_ADMIN_EMAIL=your-email@example.com
# ATLASSIAN_ADMIN_API_TOKEN=your-admin-api-token

# -----------------------------------------------------------------------------
# LLM Provider (Required for Stage 5 and Two-Stage Retrieval)
# -----------------------------------------------------------------------------
DEEPSEEK_API_KEY=your-deepseek-api-key

# -----------------------------------------------------------------------------
# GitHub (Optional — enables Stage 3b codebase context)
# -----------------------------------------------------------------------------
GITHUB_TOKEN=your-github-personal-access-token

# -----------------------------------------------------------------------------
# Jira Custom Fields (Optional — instance-specific field IDs)
# -----------------------------------------------------------------------------
# These map your Jira custom fields to the pipeline. Find your field IDs at:
# https://your-domain.atlassian.net/rest/api/3/field
#
# JIRA_PROJECT_DROPDOWN_FIELD=customfield_10072   # "Project" dropdown field
# JIRA_PROJECT_TEXT_FIELD=customfield_10073        # "Project" text fallback
# JIRA_PROJECT_LINK_FIELD=customfield_10107        # "Project Link" URL field
